name: 'My First Drupal Custom Module'
description: 'Practice the custom module building'
type: module
package: Custom
core_version_requirement: ^9 || ^10

dependencies:
  # add a core module dependency
  - drupal:views_ui
  # add a contrib module dependency
  - pathauto:pathauto
  # add a submodule dependency
  - devel:devel_generate

# Try to add the configure button in extend page, but it is not showing
# correctly I think it occurs as I dont add the routing file.
configure: mymodule.settings
